# Local usability results

Raw participant session JSON files belong in this directory and are excluded
from Git. Use pseudonymous IDs and never store credentials, production
addresses, personal information, or customer incident data.

Generate a reviewed aggregate report under `docs/test-results/` when the study
is complete.

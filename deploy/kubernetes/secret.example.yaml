apiVersion: v1
kind: Secret
metadata:
  name: net-sec-watch-secrets
  namespace: net-sec-watch
type: Opaque
stringData:
  OPENSEARCH_INITIAL_ADMIN_PASSWORD: "replace-in-secret-manager"
  OIDC_CLIENT_SECRET: "replace-in-secret-manager"
  KEYCLOAK_ADMIN_PASSWORD: "replace-in-secret-manager"
